# Repository profile

This repository owns the immutable Codestra Grafana configuration image. The image is a repository-built, signed overlay on the exact official Grafana 13.2.1 digest. The vendored unreleased upstream snapshot is retained for reference and is not executable authority.

Production activation remains disabled. Promotion, release, secrets delivery, routing, and deployment require separate protected-environment evidence.
